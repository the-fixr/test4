# ============================================
# ENVIRONMENT VARIABLES
# ============================================
# Copy this file to .env.local and fill in your values
#
# SECURITY WARNING:
# - NEVER commit .env.local to git
# - NEVER expose API keys in client-side code
# - Use NEXT_PUBLIC_ prefix ONLY for non-sensitive values
# ============================================

# Your deployed app URL (required for Farcaster metadata)
NEXT_PUBLIC_APP_URL=https://your-app.vercel.app

# ============================================
# OPTIONAL: API KEYS (server-side only)
# ============================================
# These are used for server-side API routes only.
# NEVER prefix these with NEXT_PUBLIC_

# Token analysis (if using TokenAnalyzer module)
# GECKOTERRMINAL_API_KEY=your_key_here
# GOPLUS_API_KEY=your_key_here

# ============================================
# SECURITY CHECKLIST
# ============================================
# Before deploying:
# [ ] .env.local is in .gitignore
# [ ] No API keys have NEXT_PUBLIC_ prefix
# [ ] All URLs use HTTPS
# [ ] manifest.json URLs match your domain
